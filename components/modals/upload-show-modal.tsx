import {Text} from "@mantine/core";
import {ContextModalProps} from "@mantine/modals";

export const UploadShowModal = ({
                                    context,
                                    id,
                                }: ContextModalProps) => (
    <>
        <Text>hi</Text>
    </>
);
